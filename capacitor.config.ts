import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.surtilider.SurtiVentas',
  appName: 'SurtiVentas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
